<UserControl x:Class="Sakuno.KanColle.Amatsukaze.Views.Game.Overview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:rb="clr-namespace:Sakuno.KanColle.Amatsukaze;assembly=HeavenlyWind.Base"
             xmlns:rvmg="clr-namespace:Sakuno.KanColle.Amatsukaze.ViewModels.Game"
             xmlns:rvg="clr-namespace:Sakuno.KanColle.Amatsukaze.Views.Game"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <Style x:Key="PanelBorderStyle" TargetType="{x:Type Border}">
            <Setter Property="BorderBrush" Value="{DynamicResource HighlightBorderBrushKey}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Margin" Value="0, 0, 0, 8" />
        </Style>
        <Style x:Key="PanelCaptionStyle" TargetType="{x:Type Border}">
            <Setter Property="Background" Value="{DynamicResource HighlightBackgroundBrushKey}" />
            <Setter Property="Padding" Value="4" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="8" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource PanelBorderStyle}" Grid.ColumnSpan="3">
            <StackPanel>
                <Border Style="{StaticResource PanelCaptionStyle}">
                    <TextBlock Text="{rb:StringResource Main.Overview_Headquarter}" />
                </Border>

                <rvg:Headquarter />
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="1">

            <Border Style="{StaticResource PanelBorderStyle}">
                <StackPanel>
                    <Border Style="{StaticResource PanelCaptionStyle}">
                        <TextBlock Text="{rb:StringResource Main.Overview_Fleets}" />
                    </Border>

                    <rvg:FleetsOverview />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource PanelBorderStyle}">
                <StackPanel>
                    <Border Style="{StaticResource PanelCaptionStyle}">
                        <TextBlock Text="{rb:StringResource Main.Overview_ExecutingQuests}" />
                    </Border>

                    <rvg:ExecutingQuestsOverview />
                </StackPanel>
            </Border>

        </StackPanel>

        <StackPanel Grid.Column="2" Grid.Row="1">

            <Border Style="{StaticResource PanelBorderStyle}">
                <StackPanel>
                    <Border Style="{StaticResource PanelCaptionStyle}">
                        <TextBlock Text="{rb:StringResource Main.Overview_RepairDocks}" />
                    </Border>

                    <rvg:RepairDocks />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource PanelBorderStyle}">
                <StackPanel>
                    <Border Style="{StaticResource PanelCaptionStyle}">
                        <TextBlock Text="{rb:StringResource Main.Overview_ConstructionDocks}" />
                    </Border>

                    <rvg:ConstructionDocks />
                </StackPanel>
            </Border>

        </StackPanel>
    </Grid>
</UserControl>
