<ContentDialog
    x:Class="Sakuno.KanColle.Amatsukaze.UWP.Views.Settings.FontPickerDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Sakuno.KanColle.Amatsukaze.UWP.Views.Settings"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Uid="/Resources/FontPicker">
    <ListBox ItemsSource="{x:Bind Source}" SelectedValue="{x:Bind SelectedFont, Mode=TwoWay}">
        <ListBox.ItemContainerStyle>
            <Style TargetType="ListBoxItem">
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            </Style>
        </ListBox.ItemContainerStyle>
        <ListBox.ItemTemplate>
            <DataTemplate x:DataType="local:FontSelection">
                <Grid>
                    <TextBlock x:Uid="/Resources/SampleText" FontFamily="{x:Bind FontFamily}"/>
                    <TextBlock Text="{x:Bind Display}" HorizontalAlignment="Right"/>
                </Grid>
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
</ContentDialog>
